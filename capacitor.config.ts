import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lebanonbakery.dashboard',
  appName: 'LebanonBakeryDashboard',
  webDir: 'dist'
};

export default config;
